name: Build and Push Docker Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (core-engine)
        id: meta_engine
        uses: docker/metadata-action@v5
        with:
          images: docker.io/ameyk09/partpilot-engine
          tags: |
            type=ref,event=branch
            type=sha,format=short

      - name: Build and push (core-engine)
        uses: docker/build-push-action@v5
        with:
          context: ./core-engine
          push: true
          tags: ${{ steps.meta_engine.outputs.tags }}
          labels: ${{ steps.meta_engine.outputs.labels }}

      - name: Extract metadata (api)
        id: meta_api
        uses: docker/metadata-action@v5
        with:
          images: docker.io/ameyk09/partpilot-api
          tags: |
            type=ref,event=branch
            type=sha,format=short

      - name: Build and push (api)
        uses: docker/build-push-action@v5
        with:
          context: ./api
          push: true
          tags: ${{ steps.meta_api.outputs.tags }}
          labels: ${{ steps.meta_api.outputs.labels }}